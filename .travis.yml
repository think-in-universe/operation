language: python
python:
  - '3.6'

install:
  - 'pip install pipenv'
  - bash scripts/_install.sh

script:
  - travis_wait 45 bash scripts/_daily.sh

branches:
  only:
    - dark

cache:
  packages: yes
  directories:
    - $HOME/virtualenvs
    - $HOME/.local/share/virtualenvs
